<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Cinemapp" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_mid</item>
        <item name="colorPrimaryDark">@color/gray_bg</item>
        <item name="colorAccent">@color/gray_menu</item>
        <!-- Customize your theme here. -->
    </style>
</resources>